# Streamlit Cloud install target.
# Mirrors pyproject.toml, with `ollama` kept (cheap, but never reached at
# runtime in demo mode). Set the env var M4_DEMO_MODE=1 in the hosted app's
# Secrets so app.py skips the Ollama HTTP call. See DEPLOY.md.

ollama>=0.4.0
sentence-transformers>=3.0.0
chromadb>=0.5.0
langchain-core>=0.3.0
langchain-community>=0.3.0
streamlit>=1.39.0
plotly>=5.24.0
click>=8.1.0
requests>=2.32.0
beautifulsoup4>=4.12.0

# Install the repo itself so `from m4_earnings_rag.rag import ...` resolves.
-e .
